<li>
  {{burger_name}}
  <button class="change-devour" data-id="{{id}}" data-newdevour={{devoured}}>
    {{#if devoured}}DEVOUR!{{else}}MAKE!{{/if}}
  </button>

  <button class="delete-burger" data-id="{{this.id}}">CLEAN PLATE!</button>
</li>